# تحديث موديول ذكاء المنافسين — التعليمات

## اللي حصل
فحصت باقة الموديول الجديدة اللي بعتّهالي (v1.2.0) وقارنتها بالكامل مع كل ملفات موقعك.
الخبر الحلو: **95% من الموديول (كل الـ Services والـ Models والـ Jobs
والـ migrations والاختبارات) موجود بالفعل على موقعك ومطابق 100% لأحدث نسخة** —
مفيش داعي أرفعها تاني. الملفات اللي فعلاً محتاجة تتحدّث هي **4 ملفات بس**:

| الملف | التغيير |
|---|---|
| `app/Controllers/CompetitorIntelligenceController.php` | استُبدل بالكامل: يحتوي المميزات الجديدة من v1.2.0 (رسم بياني لاتجاه التغييرات، بطاقة تقييم Scorecard بالرسم البياني) **+ إصلاح باج الجافاسكريبت** (كان already مُصلح في الموديول الجديد) **+ نفس تحسينات التصميم** اللي عملناها قبل كده (الألوان بقت متسقة مع ثيم الموقع الداكن) |
| `app/routes/api.php` | إضافة سطر واحد بس: راوت `compute-scorecard` الجديد. باقي الـ 561 سطر متطابقين 100% مع اللي عندك، محدش اتمسح |
| `app/Lang/en.php` | إضافة 11 مفتاح ترجمة جديد (خاصين بالـ Scorecard) في آخر الملف. باقي الملف زي ما هو |
| `app/Lang/ar.php` | نفس الحاجة بالعربي — 11 مفتاح جديد فقط |

**الملفات دي مش محتاجة تتحدّث خالص** (متطابقة بالفعل مع أحدث نسخة، اتأكدت
بمقارنة كل حرف): `AdminController.php`, `Controller.php`,
`SystemSettingsService.php`, `web.php`, وكل ملفات `Services/CompetitorIntelligence/*`
و`Models/Ci*.php` و`Jobs/*` وملفات الـ migrations والاختبارات.

## إزاي تطبّق التحديث (اسحب وأفلت)
فك الضغط عن المجلد ده، وانسخ مجلد `app` اللي جواه فوق مجلد `app` بتاع موقعك على
السيرفر — هيستبدل الـ 4 ملفات دول بس تلقائيًا، وأي ملف تاني في موقعك مش هيتلمس.

بعد الرفع: اعمل **Hard Refresh** للمتصفح (Ctrl+Shift+R) عشان تتأكد إنه مش
بيقرا نسخة جافاسكريبت قديمة متخزّنة (cache).

## تأكدت من إيه قبل ما أسلّمهولك
- ✅ فحصت توازن الأقواس `{}` و`()` في الملف بالكامل
- ✅ فحصت توازن الـ `<div>` في الـ HTML (110 فتح = 110 قفل)
- ✅ شغّلت الجافاسكريبت بالكامل على محرك Node.js للتأكد إنه بيتفهرس من غير أي خطأ نحوي
- ✅ قارنت كل مفتاح ترجمة في ملفات اللغة للتأكد إن مفيش حاجة ناقصة
- ✅ قارنت كل باقي ملفات الموديول حرف بحرف مع اللي موجود عندك بالفعل
