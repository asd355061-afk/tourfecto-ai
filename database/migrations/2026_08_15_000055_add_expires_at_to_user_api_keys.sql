-- ============================================================
-- Tourfecto - Migration: انتهاء صلاحية اختياري لمفاتيح API الشخصية
-- (Settings Center - Phase 15: Competitive Upgrade)
--
-- بيسمح للمستخدم يحدد فترة صلاحية للمفتاح وقت إنشائه (مثلاً "مفتاح
-- تجريبي ينتهي بعد 30 يوم") - نفس سلوك Vercel/Stripe API Keys.
-- لو القيمة NULL (الافتراضي) المفتاح يفضل صالح لغاية ما يُلغى يدويًا
-- زي الوضع الحالي تمامًا - مفيش أي تغيير على المفاتيح القائمة.
-- @version 1.0.0  @date 2026-08-15
-- ============================================================

ALTER TABLE `user_api_keys`
    ADD COLUMN `expires_at` TIMESTAMP NULL DEFAULT NULL COMMENT 'انتهاء صلاحية اختياري - NULL = لا ينتهي أبدًا' AFTER `last_used_at`;
