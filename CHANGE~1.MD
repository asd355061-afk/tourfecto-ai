# اللي اتعمل من tourfecto_profile_changes__1_.zip (النسخة v2)

## ⚠️ الموديول ده أكبر بكتير من اللي قبله (10 مراحل!)
جوه الملف `PROFILE_CHANGELOG.md` بتاعه (888 سطر) بيغطي 10 مراحل مختلفة.
قارنت كل حاجة فيه مع نسختك الحالية (اللي فيها بالفعل كل تعديلات
`tourfecto_profile_changes.zip` الأولى) ولقيت الآتي:

## 🔁 حاجات مكررة تمامًا مع اللي عملناه قبل كده (اتجاهلت بالكامل)
- **Profile fields (Currency/Country/Timezone/Language)** — عملناها في
  الجولة اللي فاتت بنفس التصميم بالظبط
- **2FA كامل + Admin Reset** — عملناها بالفعل بتصميم شبه مطابق حرفيًا
  (نفس أسماء الدوال، نفس منطق TOTP، نفس مسار الطوارئ للأدمن) - الموديول
  وصل لنفس الحل بشكل مستقل من غير ما يعرف إنه موجود بالفعل
- **`AdminController.php`** — الملف اللي جواه بيحاول يضيف
  `resetUserTwoFactor()` اللي عندك بالفعل حرفيًا - **متضمّنش في الباتش
  ده خالص** لأنه مفيهوش أي حاجة جديدة فعليًا (باقي الفروق كانت نسخة
  قديمة ناقصة تعديلات سابقة)
- **`Models/User.php`** — نفس القصة، كل الحقول موجودة أصلًا - **متضمّنش**

## ✅ الميزات الجديدة الحقيقية اللي هتتضاف

### 1) إصلاح ثغرة أمنية حقيقية في ربط حسابات OAuth
كان مفيش أصلًا مسار "ربط حساب Google لحسابي الحالي" آمن - أي محاولة
ربط كانت هتعدّي على مسار تسجيل الدخول العادي (ممكن تسجّلك دخول لحساب
تاني بالغلط لو الإيميل مختلف). دلوقتي فيه فحص صريح: لو الحساب مربوط
بمستخدم تاني بالفعل، بيرفض ويوضح السبب.

### 2) تاب "الحسابات المرتبطة" (`/profile/security`)
عرض حالة Google/Facebook/Microsoft/Apple + زرار ربط/فصل فعلي حقيقي
(مبني على `oauth_accounts` الموجود بالفعل).

### 3) تاب "سجل تسجيل الدخول"
آخر 20 محاولة دخول (تاريخ/جهاز/موقع/IP/نجاح أو فشل) - Read-only، مفيش
بيانات إدارية حساسة (session_id, is_impersonation, إحداثيات دقيقة) بتتعرض.

### 4) تاب "الصلاحيات" (Read-only)
يعرض الدور الحالي + قائمة الميزات المفعّلة فعليًا (مبني على نظام
Feature Flags الحقيقي `FeatureFlagService` الموجود عندك، مش بيانات وهمية)
+ رابط سياسة الخصوصية.

### 5) تصدير بيانات الحساب (Data Export - GDPR-style)
- زرار "طلب تصدير" في تاب الصلاحيات → يشغّل Job في الخلفية (Async عن
  طريق QueueManager الموجود بالفعل، مش شغل مباشر بيوقف الصفحة)
- بيصدّر: الملف الشخصي + سجل تسجيل الدخول (200 آخر محاولة) + الحسابات
  المرتبطة + بيانات وصفية عن الجلسات (device/ip/تواريخ - **بدون أي
  توكن خام**)
- **النطاق محدود عمدًا**: بيانات الحساب الأساسية بس، مش كل بيانات
  العمل (مواقع/اشتراكات/CRM) - موضّح في الملف نفسه ليه (تجنب تخمين بنية
  جداول تانية مالوش تأكد منها)
- رابط تحميل موثّق (لازم تكون صاحب الطلب + الملف "جاهز" + لسه في نطاق
  7 أيام) - الملف بره `public_html` تمامًا، مش رابط عام

### 6) تحذير اشتراك نشط قبل حذف الحساب
الحماية الأساسية (كلمة مرور + تأكيد إيميل) كانت موجودة عندك بالفعل.
المُضاف الجديد: لو عندك اشتراك مدفوع فعّال، بيوقّفك بتحذير واضح إن
الحذف مش هيلغي الاشتراك تلقائيًا عند مزوّد الدفع، ولازم تأكيد واعي
إضافي قبل الاستمرار.

### 7) ربط إعداد "استقبال إيميلات" بسلوك فعلي
`AlertService.php` (تنبيهات ذكاء المنافسين) بقى فعليًا يتأكد من
`notify_email` قبل ما يبعت - كان الإعداد ده تجميلي بحت قبل كده
(بيتخزن ومفيش كود بيقرأه في أي مكان بالمشروع).

## 🚫 تجاهلت عمدًا (خطر رجرشن حقيقي)
### Phone E.164 صارم
نفس القصة تمامًا زي المرة اللي فاتت - الكود الفعلي بيرفض أي رقم مش
بصيغة دولية (`+20...`) حتى لو المستخدم بيحفظ حاجة تانية غير الهاتف.
النسخة دي بتعمل تطبيع بسيط (شيل مسافات/شرط) لكن برضه بترفض أي رقم
محلي قديم (زي `01001234567`) - رجرشن حقيقي لأي مستخدم حالي.

### إعادة بناء "Active Sessions" (Phase 7)
عندك نظام جلسات شغّال بالفعل من جولة أقدم (`listSessions`/
`logoutSession`/`logoutOtherSessions` مبني على `RefreshToken`) -
الموديول مكانش عارف بيه وبنى نسخة موازية بأسماء مختلفة
(`revokeSession`/`revokeAllSessions`). تجاهلتها بالكامل.

## الملفات في الزip ده (13 - 12 كود + 1 migration)

### دمج جراحي
- `AuthController.php` — إصلاح ربط OAuth الآمن (`socialRedirect` +
  `completeSocialLogin`)
- `UserController.php` — كل التابات الجديدة (Connected/Activity/
  Permissions) + Data Export كامل + تحذير حذف الحساب
- `routes/web.php`, `routes/api.php` — routes الجديدة بس
  (`data-export/*`, `/profile/data-export/download/{id}`)
- `public_html/index.php` — سطر classmap واحد بس (`ExportUserDataJob.php`)
- `Services/CompetitorIntelligence/AlertService.php` — فحص
  `notify_email` قبل الإرسال

### جديد كليًا
- `Jobs/ExportUserDataJob.php`
- `database/migrations/2026_08_10_000048_create_data_export_requests_table.sql`
  (آمنة، `CREATE TABLE IF NOT EXISTS`)

### ترجمة (40 مفتاح جديد × 4 لغات، دُمجوا في نفس ملفات ar/en/fr/de
اللي عندك بالفعل)

## خطوات الرفع
1. باك أب قبل أي حاجة.
2. شغّل الـmigration الوحيدة.
3. ارفع باقي الملفات (استبدال).
4. اختبر: `/profile/security` → تاب "الحسابات المرتبطة" → جرّب ربط
   Google (لازم يربط بحسابك الحالي مش يسجّلك دخول لحساب تاني).
5. اختبر: تاب "الصلاحيات" → زرار "طلب تصدير البيانات" → استنى شوية
   وشوف هل رابط التحميل ظهر.
6. اختبر حذف حساب تجريبي عنده اشتراك فعّال - اتأكد إن التحذير بيظهر.
